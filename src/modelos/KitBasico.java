package modelos;

public class KitBasico extends ItemMochila{

    public KitBasico(String nome, double peso, double volume) {
        setNome(nome);
        setPeso(peso);
        setVolume(volume);
    }

}
